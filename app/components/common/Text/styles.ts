import { StyleSheet } from "react-native";

export default StyleSheet.create({
  title: {
    fontSize: 24,
    // Other title properties
  },
  subTitle: {
    fontSize: 18,
    // Other subTitle properties
  },
});
